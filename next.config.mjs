/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["static.wixstatic.com", "res.cloudinary.com"], // Add the external domain here
  },
};

export default nextConfig;
